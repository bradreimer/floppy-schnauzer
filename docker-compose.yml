version: "3.9"

services:
  floppy-schnauzer:
    build:
      context: .
      dockerfile: Dockerfile
    image: floppy-schnauzer:latest
    container_name: floppy-schnauzer
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: "Production"
    restart: unless-stopped
